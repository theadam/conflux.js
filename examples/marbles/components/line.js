import React from 'react'
import Marble from './marble'

export default React.createClass({

  render(){
    return (
      <svg>
        <g>
          <Marble x={0} y={0} value={1} width={32} />
          <
        </g>
      </svg>
    );
  }
});
